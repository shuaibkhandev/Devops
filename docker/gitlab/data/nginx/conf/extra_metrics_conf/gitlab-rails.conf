location /rails-metrics {
  proxy_cache off;
  proxy_http_version 1.0;
  proxy_pass  http://gitlab-workhorse/-/metrics;
      server_tokens off;
      access_log off;
      allow 127.0.0.1;
      deny all;
}
